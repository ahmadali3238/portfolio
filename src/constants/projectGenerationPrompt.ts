// ============================================================
// PROJECT GENERATION PROMPT
// ------------------------------------------------------------
// Reusable prompt shown on the admin "Add Project" page. Paste it into Claude
// inside the portfolio repo, give it a project folder path, and it returns a
// drop-in ProjectEntry object (grounded in real source code) to add to
// PROJECT_ENTRIES in src/constants/projects-data.ts.
//
// Stored as a JSON-encoded string so the embedded markdown/backticks survive
// verbatim. Edit ._prompt source and regenerate if you change the wording.
// ============================================================

export const PROJECT_GENERATION_PROMPT = "# Generate a portfolio ProjectEntry from a codebase\n\nYou are documenting a software project as an entry for my developer portfolio\n(Next.js). Every portfolio project lives in ONE file —\n`src/constants/projects-data.ts` — as an object inside the exported\n`PROJECT_ENTRIES` array (each typed `ProjectEntry`, whose definition is imported\nfrom `src/constants/data.ts`). I will give you a path to the project's source\nfolder (it may be outside this repo). Produce **ONE drop-in `ProjectEntry`\nTypeScript object**, restructured into the portfolio's case-study shape and\n**grounded in the ACTUAL source code — never invent or assume**.\n\n## Step 1 — Explore the code first (do not skip)\nInvestigate before writing. Do NOT read `node_modules`, `.next`, `.git`,\n`dist`, `build`, `venv`, or lockfiles. Use `find`/`grep` to locate:\n- All manifests: `package.json` (deps, scripts), `requirements.txt`,\n  `pyproject.toml`, `Pipfile`, `go.mod`, etc. There may be several (monorepo /\n  multi-service) — find them all.\n- `README.md` and any top-level docs / `CLAUDE.md`.\n- Schema & data layer: `prisma/schema.prisma` (count models + migrations),\n  Drizzle/SQL schema files, migrations folders, `supabase/`.\n- Structure: `app/`, `src/`, `server/`, route/API folders, workers, queues,\n  Dockerfiles, `vercel.json`, CI/infra (Terraform), env examples.\nIdentify: frameworks, languages/runtimes, DBs/ORMs, LLM providers, vector\nstores, queues, payments, auth, email, storage, third-party APIs, deployment.\n\n## Step 2 — Conform to these exact types\n```ts\ninterface ProjectCaseStudyGroup { title: string; items: string[] }\ninterface ProjectCaseStudySection {\n  id: string; eyebrow: string; title: string;\n  paragraphs?: string[]; bullets?: string[];\n  groups?: ProjectCaseStudyGroup[]; diagram?: string;\n}\ninterface ProjectEntry {\n  title: string; titleShort?: string; duration: string; image: string;\n  description: string; resumeDescription: string;\n  github: string | null; demo: string | null; demoLabel: string | null;\n  website?: string | null; websiteLabel?: string | null; status?: string;\n  featuredOnResume: boolean; category: string;\n  technologies: string[]; highlights: string[];\n  caseStudySections: ProjectCaseStudySection[];\n}\n```\n\n## Step 3 — caseStudySections (use these ids/eyebrows in this order)\n1. `overview` — eyebrow \"Overview\" — 2 `paragraphs`: what it is + how it's built.\n2. `problem` — eyebrow \"Problem\" — 3–4 `bullets`.\n3. `solution` — eyebrow \"Solution\" — 3 `bullets`.\n4. `features` — eyebrow \"Core Features\" — 3–5 `groups`, each `{title, items[]}`.\n5. `architecture` — eyebrow \"Architecture\" — a `diagram` string (multi-line,\n   `\\n` between steps, `→` prefixes, e.g. `\"Users\\n→ Frontend\\n→ API\\n→ DB\"`),\n   plus 1–2 `bullets` of detail.\n6. `engineering` — eyebrow \"Technical Highlights\" or \"Engineering Highlights\" —\n   either `bullets` or `groups`; lead with concrete, verified facts (model\n   counts, endpoint counts, queue/retry behavior, security middleware).\n7. `impact` — eyebrow \"Business Value\" or \"Impact\" — 3 `bullets`.\n\n## Field rules\n- `description`: one sentence (card subtitle). `resumeDescription`: one dense\n  résumé-style sentence.\n- `technologies`: only what you VERIFIED in manifests/config. Do not list\n  installed-but-unused packages as if they're used.\n- `highlights`: exactly 3, punchy, each carrying a verified metric or\n  differentiator.\n- `title`/`titleShort`/`category`: you choose, matching the portfolio's voice\n  (e.g. \"Navero — AI Hiring Operating System\", category \"AI Hiring Platform\").\n- `image`: `\"/<kebab-titleShort>.webp\"`.\n- Metadata I must give you (ask if I haven't): `duration`, `featuredOnResume`,\n  and whether `github`/`demo` are public links or `null`. If the repo is\n  private or has no demo, set them `null` and add `status` (e.g.\n  `\"Private repository\"`, `\"Not publicly released\"`).\n\n## Accuracy\n- Every number and claim must trace to the code. Report ACTUAL counts.\n- If the README contradicts the code (e.g. claims a provider/integration that\n  isn't wired, or that's installed but unused), document the **code truth** and\n  note the discrepancy briefly in the relevant `impact`/`engineering` bullet.\n- Third-person, outcome-focused, concise. No marketing fluff.\n\n## Output\nReturn ONLY the `ProjectEntry` object as valid TypeScript, ready to paste as a\nnew element of the `PROJECT_ENTRIES` array in `src/constants/projects-data.ts`\n(place it near the other featured projects, before the older ones). Then also\nsave the matching card image to `public/<kebab-titleShort>.webp` (a branded\nsquare WebP in the same style as the existing project cards). No surrounding\nprose.\n\n---\n**Project folder:** <PASTE PATH HERE>";
